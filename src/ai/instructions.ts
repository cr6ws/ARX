export const UI_RULES = [
  "Never log secrets or raw passwords.",
  "Only reveal passwords after explicit user action.",
  "Always keep the vault locked when idle.",
  "Do not persist secrets in UI state longer than needed.",
];
